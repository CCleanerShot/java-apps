# Requirements
- Java 21
- Gradle
- Maven

# Setup
- `cd` into `./scripts`
- Run `test-all.sh`
- Optionally, add a parameter of either `maven` or `gradle` to run with either (`test-all.sh raven`)

# TODO
- error handling for user + internal
- parameterized tests
- maven wrapper
- gradle wrapper