# Requirements
- Java 21
- Gradle
- Maven

# Setup
- `cd` into `./scripts`
- Run `test-all.sh`
- Optionally, add a parameter of either `maven` or `gradle` to test/run only with either (`test-all.sh raven`)